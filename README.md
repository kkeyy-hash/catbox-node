# Node example of the Catbox NPM Package
- This reads every file in a specified directory and then proceeds to upload it to catbox, when the action is done successfully, it will output the URL.
- The directory can be modified on Line 10 in `main.js`: `const Directory = "./Videos";`
